mixin name(arg, token)
  - var id = encode(arg)
  h1(
    class="#{prefix}-c-annotation--name",
    id="name-#{id}"
  )
    a(class="#{prefix}-c-link-to #{prefix}-js-link-to", href="#name-#{id}")= arg