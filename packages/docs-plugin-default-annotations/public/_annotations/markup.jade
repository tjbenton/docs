div(class="c-annotation c-annotation--markup")
  h4 Markup
  if token && token.states
    - var cls 
    - var ids = _.keys(token.states)
    - data = _.isArray(data) ? data : [ data ]
    each markup in data
      if ids.indexOf(markup.id) < 0
        != partial('../_helpers/frame', { data: markup })
  else
    != partial('../_helpers/frame', { data: data })
