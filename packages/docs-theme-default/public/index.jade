- var index = (((public._data || {}).pages || {}).index || {}).page
- var home = (((public._data || {}).pages || {}).home || {}).page
if index || home
  != partial('_layout/annotation', index || home)
else 
  p use <code>@page index</code> or <code>@page home</code> to get items on this page
