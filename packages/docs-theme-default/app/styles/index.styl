@import 'helpers'

// highlight($type) {
//   return '../../node_modules/highlight.js/styles/' + $type
// }
// $types = slice(highlight_types, 0, 0)
// unshift($types, slice(highlight_types, 0, 1))
// // unshift($types, slice(highlight_types, 1, 2))
// for $type in $types
//   p($type)
//   .{$type}
//     @import highlight($type)

@require 'themes/_oceanic-next.styl'
@import 'utilities'
@import 'base'
@import 'components'
@import 'scopes'
@import 'objects'
